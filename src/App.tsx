import React, { FC } from "react";
import Line from "./components/Line";

const App: FC = () => {
  return (
    <div>
      <svg>
        <Line />
      </svg>
    </div>
  );
};

export default App;
